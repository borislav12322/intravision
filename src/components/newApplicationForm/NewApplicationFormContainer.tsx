import React, { ReactElement } from 'react';
import NewApplicationForm from './NewApplicationForm';

const NewApplicationFormContainer = (): ReactElement => <NewApplicationForm />;

export default NewApplicationFormContainer;
